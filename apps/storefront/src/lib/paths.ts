export const HOME = "/"
export const CART = "/cart"
export const STORES = "/stores"
export const PRODUCTS = "/products"

const ROUTES = {
  [HOME]: HOME,
  [CART]: CART,
  [STORES]: STORES,
  [PRODUCTS]: PRODUCTS,
}

export default ROUTES
